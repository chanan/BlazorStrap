@inherits BootstrapComponentBase
@using  BlazorStrap.util

<DynamicElement Attributes="@UnknownParameters" TagName="div" class="@classname">
    @ChildContent
</DynamicElement>

@code {
    string classname
    {
        get
        {
            var check = IsCheck ? "form-check" : "form-group";
            var row = IsRow ? "row" : null;
            return $"{check} {row} {Class}".RemoveUnneededSpaces();
        }
    }
    [Parameter] private bool IsRow { get; set; }
    [Parameter] private bool IsCheck { get; set; }
    [Parameter] private string Class { get; set; }
    [Parameter] private RenderFragment ChildContent { get; set; }
}