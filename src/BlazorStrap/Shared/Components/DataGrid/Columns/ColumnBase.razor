@typeparam TGridItem
@using Microsoft.AspNetCore.Components.Rendering
@using Microsoft.AspNetCore.Components.Web.Virtualization
@implements IDisposable
@{ ColumnState!.AddColumn(this); }

@code{

    public void RenderPlaceholder(RenderTreeBuilder __builder, PlaceholderContext context)
    {
        if (VirtualPlaceholder is not null)
        {
            @VirtualPlaceholder(context)
        }
    }
}