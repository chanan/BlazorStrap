@namespace BlazorStrap
@inherits BlazorStrapBase

@if (string.IsNullOrWhiteSpace(Source))
{
    <DynamicElement @attributes="@Attributes" TagName="svg" class="@ClassBuilder">
        @ChildContent
    </DynamicElement>
}
else
{
        @Markup
}
