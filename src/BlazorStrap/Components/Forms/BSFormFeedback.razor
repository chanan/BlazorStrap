@namespace BlazorStrap
@inherits CodeBSFormFeedback

@if (IsValid || IsInvalid)
{
    <DynamicElement Attributes="@UnknownParameters" TagName="div" class="@classname">
        @ChildContent
    </DynamicElement>
}