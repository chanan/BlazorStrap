@namespace BlazorStrap.V4
@inherits BlazorStrap.Shared.Components.Content.BSSvgBase

@if (string.IsNullOrWhiteSpace(Source))
{
    <svg @attributes="Attributes" class="@ClassBuilder">
        @ChildContent
    </svg>
}
else
{
    @Markup
}
