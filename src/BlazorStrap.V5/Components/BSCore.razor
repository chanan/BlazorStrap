@inherits BlazorStrap.Shared.Components.BSCoreBase
@namespace BlazorStrap.V5
@if(HasToaster)
{
    <BSToaster/> 
}
@if (_modalBackdropRendered)
{
    <div class="modal-backdrop fade@(_backdropShown ? "show" : "")" data-blazorstrap="backdrop"></div>
}